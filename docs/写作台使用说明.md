# 写作台：记住这几件事就够了

**打开：双击项目里的「打开写作台.cmd」。** 已打开旧页面的话，刷新一次。

## 写文章

1. 点「新建文章」，直接写标题和正文。标题还没想好也能保存。
2. 停下输入约一秒，看到「已保存到本机」就保存好了。
3. 想给别人看时，点右上角「发布」→「确认发布」。底部显示「已上线」才表示网站更新完成。

下次打开会回到上次的文章和阅读位置。「最近写过」可以快速找回其他文章。

## 整理成系列

- **先建系列再写：** 左侧「我的系列」旁点「＋」，填名称；进入系列后点「在这里写一篇」，文章自动加入。
- **先写再整理：** 在文章列表勾选几篇，点「加入系列」。可以选已有系列，也可以现场新建。
- **调整顺序：** 进入系列，拖动文章排列；也可以用文章旁的上下箭头。
- **改名、写简介、收藏：** 点系列标题旁的「···」。收藏的系列排在侧栏前面。
- **移出系列：** 点文章旁的「移出」。文章仍在「全部文章」中。
- **更新博客上的目录：** 点「更新线上目录」。只发布系列信息，草稿正文不会发布。新系列至少发布一篇文章后才出现在博客中。

一篇文章可以加入多个系列。文章发布时，也会同步所有系列的名称、简介和顺序。

## 高频操作

| 想做什么 | 怎么操作 |
| --- | --- |
| 加粗、小标题、列表、链接 | 用正文上方的小工具栏；加粗也可按 Ctrl+B |
| 换行、继续列表 | 按回车；空列表项再按回车即可结束列表 |
| 插入表格 | 工具栏点表格图标；在表格里右键可以增删行列 |
| 插入图片 | 粘贴、拖入正文，或点图片图标选择文件；每张不超过 5 MB |
| 看文章效果 | 点右上角「预览」；平时不用占着半边屏幕 |
| 修改日期、摘要或所属系列 | 点「文章设置」，摘要不填会自动摘取正文 |
| 撤销、立即保存 | Ctrl+Z、Ctrl+S |
| 复用常写的结构 | 点「新建文章」旁的下拉按钮，选择模板 |
| 制作自己的模板 | 写好框架后，在「文章设置」里点「将这篇存为模板」 |
| 修改、收藏或删除模板 | 左下角「管理模板」；不影响已经写好的文章 |

日语学习只是可选模板之一，不需要每次先选文章类型。旧文章中的公式和特殊语法会保留为 Markdown 原文，点「预览」看公式排版。

## 保存、发布和私人手记

**保存只保存到这台电脑；发布才更新博客。** 发布期间可以继续写，后面的修改留到下次发布。其他草稿和其他文章未发布的正文不会跟着上传。

私人内容从左下角「私人手记」进入，用原密码解锁。写完点「加密保存到本机项目」，再点上方「发布加密手记」。私人内容只保存密文，不要把它放进普通文章或普通模板。

草稿、自定义模板和系列收藏保存在本机，换电脑不会自动带过去。换电脑前请保留整个项目文件夹，包括隐藏的 `.studio`、`.studio-backups`、`.private-backups` 和草稿目录。私人手记也可在解锁后导出加密备份。

遇到「另一个窗口修改了文章」时，先复制当前文字；重新打开后，写作台会提供保留未保存内容为另一篇草稿的入口，避免互相覆盖。

<details><summary>首次在新电脑使用</summary>

安装 Node.js、Git 和 PowerShell 7，在项目中运行一次 `npm ci`，配置此仓库的 GitHub 推送权限。之后双击「打开写作台.cmd」即可。写作台地址是 `http://127.0.0.1:4003/`，只在本机运行，不是公开网站的后台登录页。

</details>
